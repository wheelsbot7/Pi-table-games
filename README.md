# Pi-table-games

## Usage
To start the game, cd into Pi-table-games and run the following commands to sync dependencies and run the game. It will auto-detect your camera if one is connected.

```bash
# Install uv if you haven't already
# curl -LsSf https://astral.sh/uv/install.sh | sh

uv sync
uv run hand-game
```
